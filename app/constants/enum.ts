export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER'
}

export enum Status {
  AVANT = "AVANT",
  EN_COURS = "EN_COURS",
  FINI = "FINI"
}

export enum Unite {
  VRAC = "VRAC",
  UNITE = "UNITE"
}